# Replit Preview

This repository is previewed as an existing static HTML/CSS/JavaScript website.

- Run **Static website Preview** to serve the repository root at port 5000.
- Open the Replit Preview to view `index.html`.
- The Preview does not start the separate API services or change any external infrastructure.